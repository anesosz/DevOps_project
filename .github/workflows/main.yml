name: CI devops 2024

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test-backend: 
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build and test with Maven #Run SonarCloud analysis only on master branch
        if: github.ref == 'refs/heads/master' 
        run: mvn -B verify sonar:sonar -Dsonar.projectKey=anesosz -Dsonar.organization=anesosz -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=${{ secrets.SONAR_TOKEN }} --file ./backend/simpleapi
        working-directory: ./backend/simpleapi

  build-and-push-docker-image:
    needs: test-backend
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to DockerHub
        run: docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push backend Docker image
        uses: docker/build-push-action@v3
        with:
          context: ./backend/simpleapi
          tags: ${{ secrets.DOCKERHUB_USERNAME}}/tp-devops-backend:latest
          push: ${{ github.ref == 'refs/heads/master' }}

      - name: Build and push database Docker image
        uses: docker/build-push-action@v3
        with:
          context: ./database
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-database:latest
          push: ${{ github.ref == 'refs/heads/master' }}

      - name: Build and push frontend Docker image
        uses: docker/build-push-action@v3
        with:
          context: ./frontend
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-frontend:latest # Fixed typo in Docker image name
          push: ${{ github.ref == 'refs/heads/master' }}
